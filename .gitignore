# Rust / Cargo build output
/target
**/*.rs.bk

# MSVC debug info
*.pdb

# Criterion output
/criterion

# NNUE nets and datagen output (large binaries; store out of tree)
*.nnue
/data
/nets

# Editor / OS cruft
.DS_Store
*.swp
.idea/
.vscode/

# SPRT baseline binaries (large, rebuildable from the tagged commit)
/baseline/*.exe

# Generated opening books
/books/

# Trainer crate build output (it is its own workspace)
/trainer/target

# Local copies of external tools (bullet-utils), and the upload bundle
/tools
/dist

# Match logs and PGNs, and the tallies fastchess leaves in its working directory
/sprt
/config.json
